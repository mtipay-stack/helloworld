<!DOCTYPE html>
<html>
  <head>
    <title>Mynewapplication</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>

    <!-- Bootstrap CSS CDN -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    >
  </head>

  <body>
    <!-- Modern Navbar with gradient, logo, hover effects, and dark mode -->
    <nav class="navbar navbar-expand-lg fixed-top custom-navbar shadow-sm">
      <div class="container">
        <!-- Brand + Logo -->
        <a class="navbar-brand d-flex align-items-center fw-bold" href="<%= root_path %>">

          <%= image_tag "logo.png", alt: "School Logo", size: "42x42", class: "me-2 rounded shadow-sm" %>
          <span class="fs-5 text-light">School Manager</span>
        </a>

        <!-- Navbar toggle (mobile) -->
        <button class="navbar-toggler text-light" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navigation links -->
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item"><%= link_to "Students", students_path, class: "nav-link px-3 text-light fw-semibold" %></li>
            <li class="nav-item"><%= link_to "Teachers", teachers_path, class: "nav-link px-3 text-light fw-semibold" %></li>
            <li class="nav-item"><%= link_to "Sections", sections_path, class: "nav-link px-3 text-light fw-semibold" %></li>
            <li class="nav-item"><%= link_to "Departments", departments_path, class: "nav-link px-3 text-light fw-semibold" %></li>
          </ul>

          <!-- 🌙 Dark Mode Toggle Button -->
          <button id="themeToggle" class="btn btn-outline-light btn-sm ms-3">
            🌙 Dark Mode
          </button>
        </div>
      </div>
    </nav>

    <!-- Add spacing below fixed navbar -->
    <div style="margin-top: 80px;"></div>

    <!-- Flash messages -->
    <% flash.each do |key, message| %>
      <div class="alert alert-<%= key == "notice" ? "success" : "danger" %> alert-dismissible fade show" role="alert">
        <%= message %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% end %>

    <!-- Main page content -->
    <%= yield %>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
